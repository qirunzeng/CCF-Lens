# Safari host app

Run `xcodegen generate` in this directory to regenerate `CCF Lens.xcodeproj` from `project.yml`. Open the project in a full Xcode installation, select your signing team for both targets, and run the `CCF Lens` scheme.

The app opens Safari's Extensions settings. Enable CCF Lens and grant access only to the three supported publication sites.
