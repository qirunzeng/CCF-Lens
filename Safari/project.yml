name: VenueRank Lens
options:
  bundleIdPrefix: com.qirunzeng
  deploymentTarget:
    macOS: "13.0"
settings:
  base:
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: TC3755P2NZ
    CURRENT_PROJECT_VERSION: "1"
    MARKETING_VERSION: "1.2.0"
    SWIFT_VERSION: "5.0"
targets:
  VenueRank Lens:
    type: application
    platform: macOS
    sources:
      - App
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.qirunzeng.CCFLens
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_FILE: App/Info.plist
        ENABLE_APP_SANDBOX: YES
        ENABLE_HARDENED_RUNTIME: YES
        INFOPLIST_KEY_CFBundleDisplayName: VenueRank Lens
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.utilities
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    dependencies:
      - target: VenueRank Lens Extension
        embed: true
  VenueRank Lens Extension:
    type: app-extension
    platform: macOS
    sources:
      - Extension
      - path: ../WebExtension
        type: folder
      - path: ../manifest.json
    info:
      path: Extension/Info.plist
      properties:
        CFBundleDisplayName: VenueRank Lens
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSExtension:
          NSExtensionPointIdentifier: com.apple.Safari.web-extension
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).SafariExtensionHandler
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.qirunzeng.CCFLens.Extension
        PRODUCT_NAME: VenueRank Lens Extension
        ENABLE_APP_SANDBOX: YES
        ENABLE_HARDENED_RUNTIME: YES
        SKIP_INSTALL: YES
schemes:
  VenueRank Lens:
    build:
      targets:
        VenueRank Lens: all
        VenueRank Lens Extension: all
    run:
      config: Debug
